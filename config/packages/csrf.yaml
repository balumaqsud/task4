# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        # Note: login and logout use session CSRF. Stateless double-submit cookies
        # break the second Sign In after Turbo consumes the cookie.
        stateless_token_ids:
            - submit
